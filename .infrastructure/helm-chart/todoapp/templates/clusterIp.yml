apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-service
  namespace: {{ .Values.todoapp.namespace }}
  labels:
    job: todoapp
    app: service-api
spec:
  type: ClusterIP
  selector:
    app: todoapp
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080